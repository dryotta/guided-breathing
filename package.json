{
  "name": "guided-breathing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Responsive guided breathing app built around the physiological sigh.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  }
}
